# 배포 테스트 진행 상황

## 1. 백엔드 소스 코드 업로드 대상 분류
- **업로드해야 할 파일 및 디렉토리:**
    - `Dockerfile`
    - `pyproject.toml`
    - `uv.lock`
    - `src/` 디렉토리 내의 Python 소스 코드 파일 및 디렉토리 (`__init__.py`, `main.py`, `streamlit_app.py`, `agents/`, `DB/`, `LangGraphScripts/`, `RAG/`, `STT/`, `학습/` 등)
- **업로드에서 제외해야 할 파일 및 디렉토리:**
    - `src/__pycache__/`
    - `src/.git/`
    - `src/accident_response_chatbot.egg-info/`

## 2. Flutter 앱 백엔드 API 주소 변경
- **수정 파일:** `front/lib/chat.dart`
- **수정 내용:**
    - `_ChatbotScreenState` 클래스 내에 다음 `baseUrl` 상수를 추가:
      ```dart
      static const String baseUrl = "https://chatbot-backend-599050237852.asia-northeast3.run";
      ```
    - `_callBackendAnalysis` 메서드 내에서 API 호출 URL을 `http://localhost:8001/analyze` 에서 `$baseUrl/analyze` 로 변경하여 새로 정의된 `baseUrl`을 사용하도록 수정.

## 3. 프론트엔드 백엔드 연동 준비 확인
- Flutter 프론트엔드는 이제 `https://chatbot-backend-599050237852.asia-northeast3.run` 주소의 백엔드와 통신할 준비가 완료되었습니다. 앱 실행 시 해당 URL로 API 요청을 보낼 것입니다.

---

## 4. `ERR_NAME_NOT_RESOLVED` 오류 발생 및 해결 (프론트엔드)
- **오류 원인:** Flutter 앱에서 호출하는 백엔드 URL에 `.app`이 누락되어 DNS 확인 실패(`net::ERR_NAME_NOT_RESOLVED`) 발생.
- **해결 내용:** `front/lib/chat.dart` 파일의 `baseUrl`을 `https://chatbot-backend-599050237852.asia-northeast3.run.app`으로 수정.

## 5. 백엔드 `BACKEND_URL` 및 `CHAT_URL` 업데이트 (백엔드 Python 스크립트)
- **수정 파일:**
    - `src/LangGraphScripts/chatbot_frontend.py`
    - `src/streamlit_app.py`
    - `src/STT/STT_frontend.py`
    - `src/STT/STT_RAG_frontend.py`
- **수정 내용:** 각 파일에서 `BACKEND_URL` 및 `CHAT_URL` 변수의 도메인 부분을 `http://127.0.0.1:8000` 에서 `https://chatbot-backend-599050237852.asia-northeast3.run.app`으로 변경. (각 스크립트의 기존 엔드포인트는 유지).

## 6. Flutter 프론트엔드 채팅 기능 통합
- **수정 파일:** `front/lib/chat.dart`
- **수정 내용:**
    - `_inChatMode` (채팅 모드 활성화 여부) 및 `_currentThreadId` (대화 세션 ID) 상태 변수 추가.
    - `/chat` 엔드포인트로 POST 요청을 보내는 `_callBackendChat` 함수 구현.
    - `_analyzeWithGPT` 함수 수정:
        - `ResultScreen`으로의 `Navigator.push` 제거.
        - 분석 완료 후 `threadId`를 `_currentThreadId`에 저장하고 `_inChatMode = true` 설정.
        - 분석 결과를 보기 좋게 포맷하여 봇 메시지로 채팅 화면에 직접 표시.

## 7. 백엔드 헬스 체크 결과
- **확인 내용:** `https://chatbot-backend-599050237852.asia-northeast3.run.app/health` URL 접속 시 `{"status":"healthy", ...}` 응답 확인.
- **결론:** 백엔드 서비스는 현재 정상적으로 동작하고 접근 가능한 상태임이 확인되었습니다.

---

## 8. 현재까지 진행된 수정 사항 (2026-02-08)

### 8.1. 프론트엔드 통신 및 라우팅 개선
- **`front/lib/chat.dart` 수정:**
    - `_callBackendChat` 함수가 `application/x-www-form-urlencoded` 대신 `application/json` 형식으로 데이터를 전송하도록 변경.
    - `baseUrl`을 하드코딩하지 않고 `front/.env` 파일에서 `BACKEND_URL` 환경 변수를 불러오도록 수정.
    - `_analyzeWithGPT` 함수가 분석 완료 후 `ResultScreen`으로 `Navigator.push`를 통해 이동하고, 백엔드 응답을 `analysisResult`로 전달하도록 로직 변경.
    - `_analyzeWithGPT` 및 `_callBackendAnalysis` 함수 진입 시 디버깅용 `print` 로그 추가.
- **`front/.env` 파일 생성:** `BACKEND_URL=https://chatbot-backend-599050237852.asia-northeast3.run.app` 내용을 포함하여 생성.
- **`front/lib/result.dart` 수정:**
    - '챗봇 진행' 버튼 클릭 시 `ChatWidgetScreen` 대신 `ChatbotScreen`으로 이동하도록 변경 및 import문 수정.
    - `ResultScreen`에서 `analysisResult` (백엔드 응답 전체)를 받아, `result` 키 아래의 실제 분석 데이터(`fault_ratio`, `reasoning`, `legal_basis`)를 올바르게 파싱하여 화면에 표시하도록 로직 수정.
    - `referencesList` 렌더링 시 `TypeError: "source": type 'String' is not a subtype of type 'int'` 오류 방지를 위해, 각 참조 객체가 `Map<String, dynamic>` 타입인지 확인하고 안전하게 접근하도록 보강.

### 8.2. 사용자 경험 개선
- **`front/lib/chat.dart` 수정:**
    - '사고 상황 UI 입력'에서 마지막 '추가 정보' (`additional_info`) 입력 필드가 선택 사항이 되도록 수정. 텍스트 입력 없이도 분석을 진행할 수 있으며, 빈 입력 시 "(입력 없음)"으로 표시.

### 8.3. 서버 권한 설정 (수동 필요)
- `gcloud run services add-iam-policy-binding chatbot-backend --region asia-northeast3 --member="allUsers" --role="roles/run.invoker"` 명령어를 통해 Google Cloud Run 서비스에 대한 호출 권한을 부여해야 함. 이 작업은 CLI 환경에서 자동 수행이 불가능하여 사용자가 수동으로 진행해야 함.

---

## 9. 현재 문제 및 다음 단계

- **현재 문제:**
    - `ResultScreen`이 분석 결과를 문제 없이 표시하고, 챗봇 기능도 정상적으로 작동함이 확인됨.
    - 그러나, Flutter 앱에서 백엔드로의 초기 `ClientException: Failed to` 연결 문제에 대한 명확한 원인 분석은 아직 완료되지 않음. 특히, 디버깅용 `print` 로그가 브라우저 콘솔에 나타나지 않는 문제가 있어, 실제 네트워크 오류 상황 파악이 어려움.
- **다음 단계:**
    - 사용자에게 Flutter 웹 애플리케이션 실행 시 **브라우저 콘솔에 추가된 디버깅 `print` 로그**(`💡 _analyzeWithGPT 함수 호출됨.`, `💡 _callBackendAnalysis 함수 호출됨.`)가 제대로 출력되는지 확인 요청.
    - 만약 로그가 여전히 표시되지 않는다면, Flutter 웹 앱의 빌드/실행 환경 또는 브라우저 콘솔 설정에 대한 추가 정보 확인 필요.
    - `gcloud` 명령어 수동 실행 완료 여부 확인.
    - **명확한 에러 로그가 확보되면, 이를 바탕으로 네트워크 연결 문제를 추가 분석 진행.**

## 10. Gemini 챗봇 문의를 위한 관련 파일
- **`front/lib/chat.dart`**: Flutter 프론트엔드의 API 호출 로직 및 UI 흐름을 담당합니다.
- **`front/lib/result.dart`**: 분석 결과를 표시하는 화면을 담당합니다.
- **`front/.env`**: 백엔드 URL 환경 변수를 포함합니다.
- **`src/main.py`**: FastAPI 백엔드의 엔드포인트 정의 및 요청 처리 로직을 포함합니다.