# 2026년 2월 6일 Gemini CLI 세션 기록

## 1. 초기 설정 및 프로젝트 구조 확인
- Gemini CLI 설정 완료 메시지 확인.
- `ls` 명령을 통해 `/home/snake/project/chatbot/` 디렉토리 구조 확인.

## 2. 백엔드 TypeError 문제 해결
- 백엔드 응답에서 `TypeError: "N/A": type 'String' is not a subtype of type 'Map<String, dynamic>?'` 오류 발생.
- `front/lib/main.dart` 및 `front/lib/chat.dart` 파일 분석.
- **원인**: `chat.dart`에서 `analysisResultData['suggested_fault_ratio']`를 찾았으나 백엔드는 `fault_ratio` 키를 사용했고, `null`일 경우 `'N/A'` 문자열을 할당하여 `Map<String, dynamic>` 타입 불일치 발생.
- **해결**: `front/lib/chat.dart` 수정:
    - `fault_ratio` 키로 변경.
    - `analysis` 키를 백엔드의 `reasoning`으로 변경.
    - `fault_ratio`를 `Map<String, dynamic>?` 타입으로 명시적 캐스팅.

## 3. 백엔드 연동 확인
- `src/LangGraphScripts/chatbot_backend copy.py` 파일 분석.
- `analyze` 엔드포인트의 요청/응답 구조가 프론트엔드와 일치함을 확인, 연동 문제 없음.

## 4. 프론트엔드 실행 및 초기 문제 해결
- `flutter pub get` 명령 실행 시 `Expected to find project root in current working directory.` 오류 발생 -> `dir_path` 지정으로 해결.
- `flutter run -d web-renderer html` 명령 실행 시 `Target file "html" not found.` 오류 발생 -> 명령 구문 오류 (`--web-renderer` 옵션 사용법).
- `flutter run -d web --web-renderer html` 명령 실행 시 `Could not find an option named "--web-renderer".` 오류 발생 -> Flutter 버전 또는 옵션 호환성 문제.
- `flutter run -d web` 명령 실행 시 `No supported devices found with name or id matching 'web'.` 오류 발생 -> 장치 ID 오류.
- `flutter run -d chrome` 명령 실행으로 앱 성공적으로 실행 (다만 "Application finished." 메시지 발생).
- **해결**: `flutter clean`, `flutter pub get` 후 `flutter run -d chrome` 명령으로 성공적인 앱 재실행 확인.

## 5. PDF 저장 관련 폰트 문제 해결
- PDF 저장 버튼 클릭 시 `ERR_CONNECTION_REFUSED`, `Unable to load asset` 오류 발생.
- **원인**: `front/pubspec.yaml`에 폰트 파일이 개별 파일로 등록되어 경로 중복 문제 발생 (`assets/assets/fonts/`).
- **해결**: `front/pubspec.yaml` 수정:
    - `assets/fonts/NotoSansKR-Regular.ttf`를 `assets/fonts/`로 변경하여 디렉토리 전체 등록.
- PDF 저장 시 한글 깨짐 오류 발생 (`Helvetica has no Unicode support`, `Unable to find a font to draw "사"`).
- **원인**: `pdf` 패키지가 기본 Helvetica 폰트로는 한글을 지원하지 않으며, 특히 굵은 글씨체(`Helvetica-Bold`)에 한글 폰트가 적용되지 않아 발생. `NotoSansKR-Bold.ttf` 폰트 파일이 `front/assets/fonts/` 디렉토리에 누락됨.
- **해결**:
    - **사용자에게 Noto Sans KR 폰트 패밀리 다운로드 및 `NotoSansKR-Bold.ttf` 파일을 `front/assets/fonts/`에 추가하도록 요청.**
    - `front/lib/result.dart` 수정: 일반 폰트(`NotoSansKR-Regular.ttf`)와 굵은 폰트(`NotoSansKR-Bold.ttf`)를 모두 로드하여 `pw.ThemeData.withFont(base: ttf, bold: boldTtf)`를 통해 PDF 문서 전체에 기본 폰트로 적용.

## 6. 분석 결과 화면 - 참고 자료 미표시 문제 해결
- 분석 결과 화면의 "참고 자료" 섹션에 내용이 표시되지 않음.
- **원인**: `front/lib/chat.dart`에서 백엔드 응답의 `legal_references` 키를 찾았으나, 백엔드는 `legal_basis` 키를 사용하고 있었기 때문에 데이터 불일치 발생.
- **해결**: `front/lib/chat.dart` 수정:
    - `analysisResultData['legal_references']`를 `analysisResultData['legal_basis']`로 변경.

## 7. 분석 결과 화면 - 게시판 저장 기능 확인
- "게시판 저장" 버튼이 백엔드와 연결되는지 확인.
- `front/lib/result.dart` 및 `front/lib/board_save.dart` 파일 분석.
- **결론**: 현재 `BoardSaveScreen`의 "게시판 저장" 버튼은 백엔드와 실제로 연결되어 있지 않으며, 임시 로컬 저장 시뮬레이션 코드만 존재. 백엔드 연동을 위한 추가 개발 필요.

## 8. 사진 입력 기능 확인 및 문제 해결
- 프론트엔드에 사진을 입력받는 부분이 있는지 확인.
- `front/lib/chat.dart`, `front/lib/main.dart`, `front/lib/photo.dart` 파일 분석.
- **결론**: `PhotoScreen`에서 사진을 촬영/선택하고 `ChatbotScreen`으로 전달하는 기능이 구현되어 있음. `main.dart`에서 `EmergencyScreen`을 통해 `PhotoScreen`으로 진입.
- 사진 업로드 시 `Unsupported operation: MultipartFile is only supported where dart:io is available.` 오류 발생 (웹 환경).
- **원인**: `http.MultipartFile.fromPath()`가 `dart:io`에 의존하여 웹에서 사용 불가.
- **해결**:
    - `front/lib/chat.dart` 수정: `ChatbotScreen`의 `accidentPhotos` 타입을 `List<File>`에서 `List<XFile>`로 변경. `_callBackendAnalysis`에서 `photo.readAsBytes()`와 `http.MultipartFile.fromBytes()` 사용.
    - `front/lib/photo.dart` 수정: `ChatbotScreen` 호출 시 `_selectedImages` (List<XFile>)를 직접 전달.
    - `front/lib/main.dart` 수정: `ChatbotScreen` 호출 시 `accidentPhotos: const <XFile>[]`로 타입 명시.
- 업로드된 이미지 미리보기 기능 미적용.
- **원인**: `Image.file` 위젯이 `dart:io`에 의존하여 웹에서 이미지 로드 불가.
- **해결**: `front/lib/photo.dart` 수정:
    - `Image.file`을 `FutureBuilder<Uint8List>`와 `Image.memory`로 교체하여 비동기적으로 이미지 데이터(`XFile.readAsBytes()`)를 읽어와 표시.
    - `front/lib/photo.dart`에 `import 'dart:typed_data';` 추가.

---
**다음 단계:** 모든 수정사항이 적용되었으므로, 앱을 다시 시작하고 위에 나열된 모든 기능(사진 다중 선택 및 미리보기, PDF 저장, 백엔드 분석)을 철저히 테스트해야 합니다. 특히 PDF 저장 시 한글 폰트 문제가 완전히 해결되었는지 확인하는 것이 중요합니다.
